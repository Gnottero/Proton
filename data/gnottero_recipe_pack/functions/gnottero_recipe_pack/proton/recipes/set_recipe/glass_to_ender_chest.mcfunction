#> Add the "" tag to the executor and put the result item in the result slot
    #> Called by the "" function

    #> Put the result item in the result slot
        loot replace block ~ ~ ~ container.15 loot gnottero_recipe_pack:proton/recipes/glass_to_ender_chest

    #> Add the "gn.is_crafting" tag to the executor
        tag @s add gn.is_crafting
